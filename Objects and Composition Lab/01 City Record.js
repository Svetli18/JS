function solve(townName, population, treasury){
    const city = {name: "", population: 0, treasury: 0}

    city.name = townName;
    city.population = population;
    city.treasury = treasury;

    return city;
}

solve('Tortuga', 7000, 15000
);